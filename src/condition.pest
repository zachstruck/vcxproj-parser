WHITESPACE = _{ " " | "\t" | "\r" | "\n" }
simple_string = { ASCII_ALPHANUMERIC+ }
lparen = _{ "(" }
rparen = _{ ")" }
group = { lparen ~ term ~ rparen }
term = { group | eq }
eq = { simple_string ~ "==" ~ simple_string }
main = { SOI ~ term ~ EOI }
